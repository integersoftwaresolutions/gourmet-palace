# Local development normally uses the Vite /api proxy, so this can be omitted.
# On Vercel (same-origin deploy) keep /api/v1 so the browser talks to this deployment.
# Only set a full URL if the API is hosted on a different origin.
VITE_API_BASE=/api/v1
