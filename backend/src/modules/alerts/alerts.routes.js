const {Router}=require('express');const ApiResponse=require('../../utils/ApiResponse');const asyncHandler=require('../../utils/asyncHandler');const {authenticate}=require('../../middlewares/auth.middleware');const svc=require('./alerts.service');const r=Router();r.use(authenticate);r.get('/',asyncHandler(async(req,res)=>ApiResponse.send(res,{data:{alerts:await svc.list(req.auth,req.query)}})));r.patch('/:id',asyncHandler(async(req,res)=>ApiResponse.send(res,{data:{alert:await svc.transition(req.auth,req.user,req.params.id,req.body)}})));module.exports=r;
