const {Router}=require('express');const ApiResponse=require('../../utils/ApiResponse');const asyncHandler=require('../../utils/asyncHandler');const {authenticate,requireAdmin}=require('../../middlewares/auth.middleware');const svc=require('./reports.service');const r=Router();r.use(authenticate,requireAdmin);r.get('/',asyncHandler(async(req,res)=>ApiResponse.send(res,{data:{report:await svc.build(req.auth,req.query)}})));r.get('/csv',asyncHandler(async(req,res)=>{const report=await svc.build(req.auth,req.query);res.type('text/csv').set('Content-Disposition',`attachment; filename="gourmet-palace-report-${report.range.from}-${report.range.to}.csv"`).send(svc.csv(report))}));module.exports=r;
